#include "main.h"
/**
 * percent - pourcentage
 * Description: description
 * @list: variable
 * Return: retourne la longueur
*/
int percent(va_list list)
{
	int count = 0;

	(void)list;
	count += _putchar('%');
	return (count);

}
